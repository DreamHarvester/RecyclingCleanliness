from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Model that determines whether plastic bottles are clean and dry enough to be recycled.',
    author='Zach Wilkins',
    license='',
)
